#ifndef SORT_TESTER_H
#define SORT_TESTER_H

#include <vector>

class SortTester {
public:
    static float testQuickSort(const std::vector<int>& array);
    static float testIntroSort(const std::vector<int>& array);
};

#endif // SORT_TESTER_H
